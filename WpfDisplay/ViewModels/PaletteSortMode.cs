namespace WpfDisplay.ViewModels;

/// <summary>
/// Sorting modes for the palette browser.
/// </summary>
public enum PaletteSortMode
{
    Name,
    DominantHue,
    Warmth,
    Brightness,
    Saturation,
    KeyCount
}
