<Window x:Class="WpfDisplay.Views.EditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDisplay.Views"
        xmlns:vm="clr-namespace:WpfDisplay.ViewModels"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Background="{DynamicResource MaterialDesignPaper}"
        mc:Ignorable="d"
        Title="EditorWindow" Height="450" Width="800" Topmost="True"
        d:DataContext="{d:DesignInstance Type=vm:IFSViewModel}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel  Orientation="Horizontal" Grid.Row="0">
            <Button Command="{Binding LoadPaletteCommand}" Margin="8">🎨 Palette</Button>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="8">
            <Button Command="{Binding RemoveSelectedCommand}" IsEnabled="{Binding SelectedIterator, TargetNullValue=false}">Remove Selection</Button>
        </StackPanel>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <Label>Transforms</Label>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <!--TODO: bind ItemsSource-->
                            <Button Margin="6" Command="{Binding AddIteratorCommand}" CommandParameter="Affine">Affine</Button>
                            <Button Margin="6" Command="{Binding AddIteratorCommand}" CommandParameter="Foci">Foci</Button>
                            <Button Margin="6" Command="{Binding AddIteratorCommand}" CommandParameter="Loonie">Loonie</Button>
                            <Button Margin="6" Command="{Binding AddIteratorCommand}" CommandParameter="Spherical">Spherical</Button>
                            <Button Margin="6" Command="{Binding AddIteratorCommand}" CommandParameter="Waves">Waves</Button>
                            <Label> . . . </Label>
                        </StackPanel>
                    </ScrollViewer>
                </Grid>
                <md:Card Grid.Column="1" Margin="4" VerticalAlignment="Stretch">
                    <local:NodeMap DataContext="{Binding}"/>
                </md:Card>
            </Grid>
            <md:Card Grid.Column="1" Padding="6" Margin="8" VerticalAlignment="Stretch">
                <local:IteratorEditor DataContext="{Binding SelectedIterator}"/>
            </md:Card>
        </Grid>
    </Grid>
</Window>
