<Window x:Class="WpfDisplay.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDisplay.Views"
        xmlns:dc="clr-namespace:IFSEngine.Model;assembly=IFSEngine"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" WindowState="Maximized"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:conv="clr-namespace:WpfDisplay.Converters"
        x:Name="mainWindow"
        d:DataContext="{d:DesignInstance Type=dc:IFS, IsDesignTimeCreatable=False}"
        >
    <Window.Resources>
        <conv:ColorTypeConverter x:Key="ColorTypeConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>

        <md:Card Grid.Row="0" Margin="8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="10" Style="{DynamicResource MaterialDesignTitleTextBlock}">TODO: replace, add gui</TextBlock>
                <Button Margin="10" Content="Reset" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Click="Button_Click_1" />
                <Button Margin="10" Content="Save JSON" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Click="Button_Click_2" />
                <Button Margin="10" Content="Load JSON" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Click="Button_Click_3" />
                <Button Margin="10" Content="Load palette" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Click="Button_Click_5" />
                <Button Margin="10" Content="Save image" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Click="Button_Click_8" />
                <CheckBox x:Name="ExplorationCheckBox" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked">Exploration mode</CheckBox>
                <CheckBox IsChecked="{Binding EnableDE}">Density Estimation</CheckBox>

                <Button Margin="10" x:Name="EditorButton" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Click="EditorButton_Click">Editor</Button>
            </StackPanel>
        </md:Card>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <!-- Effects -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <md:Card Padding="6" Margin="8">
                        <StackPanel>
                            <!--TODO: separate xaml for this panel-->
                            <TextBlock Text="Tone mapping" Foreground="Gray" FontWeight="Bold" Style="{DynamicResource MaterialDesignTitleTextBlock}" />
                            <local:ValueSlider ValueName="Brightness" MinValue="0" Increment="0.01" Value="{Binding DataContext.CurrentParams.ViewSettings.Brightness, ElementName=mainWindow}"/>
                            <local:ValueSlider ValueName="Gamma" MinValue="0" Increment="0.01" Value="{Binding DataContext.CurrentParams.ViewSettings.Gamma, ElementName=mainWindow}"/>
                            <local:ValueSlider ValueName="Gamma threshold" Increment="0.01" Value="{Binding DataContext.CurrentParams.ViewSettings.GammaThreshold, ElementName=mainWindow}"/>
                            <local:ValueSlider ValueName="Vibrancy" Increment="0.05" Value="{Binding DataContext.CurrentParams.ViewSettings.Vibrancy, ElementName=mainWindow}"/>
                            <Label>Background color</Label>
                            <xctk:ColorPicker SelectedColor="{Binding DataContext.CurrentParams.ViewSettings.BackgroundColor, ElementName=mainWindow, Converter={StaticResource ColorTypeConverter}}"/>
                        </StackPanel>
                    </md:Card>
                    <md:Card Padding="6" Margin="8">
                        <StackPanel>
                            <!--TODO: separate xaml for this panel-->
                            <!--TODO: add pictograms and tooltips-->
                            <TextBlock Text="Camera" Foreground="Gray" FontWeight="Bold" Style="{DynamicResource MaterialDesignTitleTextBlock}" />
                            <local:ValueSlider ValueName="Field Of View" MinValue="1" MaxValue="179" Increment="1" Value="{Binding DataContext.CurrentParams.ViewSettings.Camera.FOV, ElementName=mainWindow}"/>
                            <local:ValueSlider ValueName="Depth Of Field" MinValue="0" Increment="0.0001" Value="{Binding DataContext.CurrentParams.ViewSettings.Dof, ElementName=mainWindow}"/>
                            <local:ValueSlider ValueName="Focus distance" Value="{Binding DataContext.CurrentParams.ViewSettings.FocusDistance, ElementName=mainWindow}"/>
                            <local:ValueSlider ValueName="Focus area" MinValue="0" Value="{Binding DataContext.CurrentParams.ViewSettings.FocusArea, ElementName=mainWindow}"/>
                            <local:ValueSlider ValueName="Fog effect" MinValue="0" Value="{Binding DataContext.CurrentParams.ViewSettings.FogEffect, ElementName=mainWindow}"/>
                            <Label>Render Resolution</Label>
                            <Button Grid.Column="2" Content="debug" Click="Button_2x" Margin="5 0 0 0"/>
                            <CheckBox x:Name="PreviewResolutionCheckBox" Checked="PreviewResolutionCheckBox_Checked" Unchecked="PreviewResolutionCheckBox_Checked">Fit to display</CheckBox>
                        </StackPanel>
                    </md:Card>
                </StackPanel>
            </ScrollViewer>
            <!-- Render -->
            <md:Card Grid.Column="1" Padding="4" Margin="8" VerticalAlignment="Stretch">
                <DockPanel Name="Docker" >
                    <local:RenderDisplay x:Name="Host" Height="{Binding ElementName=Docker, Path=ActualHeight}" Width="{Binding ElementName=Docker, Path=ActualWidth}"/>
                </DockPanel>
            </md:Card>
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <md:Card Padding="6" Margin="8">
                        <StackPanel Orientation="Horizontal" Margin="8">
                            <Button Content="Start" Style="{DynamicResource MaterialDesignRaisedDarkButton}" Click="Button_Click"/>
                            <Label>TODO: render presets</Label>
                        </StackPanel>
                    </md:Card>
                    <local:PerformanceView x:Name="performanceView" />
                </StackPanel>
            </ScrollViewer>
        </Grid>
        <md:Card Grid.Row="2" Padding="6" Margin="8" VerticalAlignment="Stretch">
            <StackPanel>
                <TextBlock Text="Animation" Margin="4 0 0 0"  Foreground="Gray" FontWeight="Bold" Style="{DynamicResource MaterialDesignTitleTextBlock}" />
            </StackPanel>
        </md:Card>
    </Grid>
</Window>
