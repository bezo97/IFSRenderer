<Window x:Class="WpfDisplay.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfDisplay.Views"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:vm="clr-namespace:WpfDisplay.ViewModels"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit" 
        xmlns:conv="clr-namespace:WpfDisplay.Converters"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" WindowState="Maximized"
        x:Name="mainWindow"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=False}"
        >
    <Window.Resources>
        <conv:DataContextSetConverter x:Key="dataContextSetConverter"/>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>

        <md:Card Grid.Row="0" Margin="8">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding NewCommand}" Margin="10">📑 New</Button>
                <Button Command="{Binding LoadParamsCommand}" Margin="10">📂 Load</Button>
                <Button Command="{Binding SaveParamsCommand}" Margin="10">💾 Save</Button>
                <Button Command="{Binding SaveImageCommand}" Margin="10">🖼 Save</Button>
                <Separator Margin="25"></Separator>
                <Button Click="EditorButton_Click" Margin="10" >🏗️ Editor</Button>
                <Button Command="{Binding LoadRandomCommand}" Margin="10">🎲 Random</Button>
            </StackPanel>
        </md:Card>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>
            <!-- Effects -->
            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <local:ToneMappingView DataContext="{Binding ToneMappingViewModel}"/>
                    <md:Card Padding="6" Margin="8">
                        <StackPanel>
                            <TextBlock Grid.Row="0"  Foreground="Gray" FontWeight="Bold" Style="{DynamicResource MaterialDesignTitleTextBlock}" >Background</TextBlock>
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="auto"/>
                                </Grid.ColumnDefinitions>
                                <xctk:ColorPicker Grid.Column="0" Margin="0,0,8,0"
                                    SelectedColor="{Binding IFSViewModel.BackgroundColor}"
                                    IsEnabled="{Binding IsColorPickerEnabled}"/>
                                <CheckBox Grid.Column="1" ToolTip="Transparent background"
                                    IsChecked="{Binding TransparentBackground}">🙾🙾</CheckBox>
                            </Grid>
                        </StackPanel>
                    </md:Card>
                    <local:CameraSettingsView DataContext="{Binding CameraSettingsViewModel}"/>
                </StackPanel>
            </ScrollViewer>
            <!-- Display -->
            <md:Card Grid.Column="1" Padding="4" Margin="8" VerticalAlignment="Stretch">
                <DockPanel Name="Docker">
                    <Grid>
                        <local:RenderDisplay
                            Visibility="{Binding Path=., Converter={StaticResource dataContextSetConverter}}"
                            x:Name="renderDisplay"
                            Height="{Binding ElementName=Docker, Path=ActualHeight}" 
                            Width="{Binding ElementName=Docker, Path=ActualWidth}"/>
                        <ProgressBar
                            Visibility="{Binding Path=., Converter={StaticResource dataContextSetConverter}, ConverterParameter=true}"
                            Width="100" Height="25"
                            IsIndeterminate="True"/>
                    </Grid>
                </DockPanel>
            </md:Card>
            <!-- Render Settings -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Hidden">
                <StackPanel>
                    <local:QualitySettingsView DataContext="{Binding QualitySettingsViewModel}"/>
                    <local:PerformanceView DataContext="{Binding PerformanceViewModel}"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        <md:Card Grid.Row="2" Padding="6" Margin="8" VerticalAlignment="Stretch">
            <StackPanel>
                <TextBlock Text="Animation" Margin="4 0 0 0"  Foreground="Gray" FontWeight="Bold" Style="{DynamicResource MaterialDesignTitleTextBlock}" />
            </StackPanel>
        </md:Card>
    </Grid>
</Window>
